[build]
builder = "nixpacks"

[deploy]
# Must match Procfile; Railway injects $PORT at runtime (shell-expanded by the platform).
startCommand = "uvicorn yomitoku_api.main:app --host 0.0.0.0 --port $PORT"
