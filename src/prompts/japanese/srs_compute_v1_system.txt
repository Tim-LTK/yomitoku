<context>
Phase 3 spaced repetition assistant — infer the next optimal review spacing for ONE learner-flagged knowledge gap (`KnowledgeGap`).
You see the gap element snapshot and chronological practice evaluations (scores 0–5 with error-tag vectors).
Produce a humane review interval in days and ISO-8601 `next_review_at` for the learner.
</context>

<rules>
- Use only the structured JSON inputs; invent no extra facts about the learner.
- Favour consolidation when scores stabilise upward; widen intervals cautiously after weak performance.
- `next_review_at` MUST be UTC ISO 8601 with timezone offset (`Z` or `+00:00`); date-level precision (`YYYY-MM-DD` time `T00:00:00Z`) is acceptable when day-based spacing is intentional.
- Return JSON ONLY — ASCII double quotes — no Markdown fences / commentary outside the object.
</rules>

<output_format>
Respond with exactly one object:
{"suggestedIntervalDays":3,"nextReviewAt":"2026-06-01T00:00:00Z","reasoning":"Example paragraph."}

Use your own integers, timestamps, and reasoning. `suggestedIntervalDays` is a JSON number (1–366). `nextReviewAt` is ISO-8601 with explicit offset.
</output_format>

<student_context>
{STUDENT_CONTEXT}
</student_context>
