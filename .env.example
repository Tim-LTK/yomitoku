# Copy to .env — never commit real secrets.
ANTHROPIC_API_KEY=
# Optional override (default: claude-sonnet-4-20250514)
# YOMITOKU_ANTHROPIC_MODEL=
# Browser CORS: comma-separated exact origins (scheme + host + optional port).
# Railway: set EXPO_WEB preview / tools first, ship builds later tighten.
ALLOWED_ORIGINS=http://localhost:8081

